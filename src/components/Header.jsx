const { Component } = require('react');

class Header extends Component {
  render() {
    return (
      <h1>
        <header>
          Sistema solar
        </header>
      </h1>
    );
  }
}

export default Header;
